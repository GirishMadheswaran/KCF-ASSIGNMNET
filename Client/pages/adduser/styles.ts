import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
      borderColor: 'black',
      borderWidth: 1,
      margin: 20,
      borderRadius: 10,
      fontSize: 15,
    },
    addEmployee: {
      margin: 20,
    },
    validationError: {
      color: 'red',
      marginLeft: 20
    },
  });